apiVersion: v2
name: sluice
description: Horizontally scalable durable job scheduler
type: application
# The release workflow overwrites both with the git tag when packaging.
version: 0.4.0
appVersion: "0.4.0"
